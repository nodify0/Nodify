rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow public read on user-specific public assets
    match /users/{userId}/{allPaths=**} {
      allow read;
      allow write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
